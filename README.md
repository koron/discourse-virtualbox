# Discourse on VirtualBox

Run discourse on VirtualBox.

How to use:

    $ vagrant up
    $ vagrant ssh

    $ sudo -s
    # cd /var/discourse/
    # ./discourse-setup
    # ./lanuncher
    # ./lanuncher bootstrap app
    # ./lanuncher start app
